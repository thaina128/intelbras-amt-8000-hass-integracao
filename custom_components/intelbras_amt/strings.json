{
  "config": {
    "step": {
      "user": {
        "title": "Intelbras AMT Alarm",
        "description": "Leave host empty to run in server mode (panel connects to Home Assistant). Fill host to run in client mode (Home Assistant connects to panel).",
        "data": {
          "host": "Host (optional)",
          "port": "Port",
          "password": "Password"
        },
        "data_description": {
          "host": "Panel IP address for client mode (example: 192.168.15.2)",
          "port": "TCP port to listen on (default: 9009)",
          "password": "Remote access password (4-6 digits)"
        }
      }
    },
    "error": {
      "invalid_port": "Invalid port number",
      "invalid_password": "Password must be at least 4 digits",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This host/port is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AMT Options",
        "data": {
          "scan_interval": "Scan Interval (seconds)"
        }
      }
    }
  }
}
